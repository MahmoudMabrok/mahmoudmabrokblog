Some needs to detect background (know that our activity-app- is in background)

# First
   use **ActivityManager** && **getRunningTasks**() and ckeck if out app is on Tasks but it became **deprecated** since Lollopop and above.

# Call backs

# JobIntentService
   